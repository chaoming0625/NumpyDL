language: python
sudo: false
python:
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - python setup.py install
  - pip install -r requirements.txt
script: py.test --runslow --cov-config=.coveragerc
after_success:
  - coveralls
